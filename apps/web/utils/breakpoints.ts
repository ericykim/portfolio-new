/**
 * Breakpoint constants that match Tailwind's default breakpoints
 * @see https://tailwindcss.com/docs/breakpoints
 */

/** Tailwind's `sm` breakpoint - 640px */
export const SM_BREAKPOINT = 640;

/** Tailwind's `md` breakpoint - 768px */
export const MD_BREAKPOINT = 768;

/** Tailwind's `lg` breakpoint - 1024px */
export const LG_BREAKPOINT = 1024;

/** Tailwind's `xl` breakpoint - 1280px */
export const XL_BREAKPOINT = 1280;

/** Tailwind's `2xl` breakpoint - 1536px */
export const XXL_BREAKPOINT = 1536;

